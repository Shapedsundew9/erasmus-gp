{
    "ORDER BY": {
        "inline": "psql_fragment_order_by({i0}, {i1})",
        "description": "PSQL ORDER BY clause with column {t0} and direction {t1}",
        "inputs": [
            "PsqlBool",
            "bool"
        ],
        "outputs": [
            "PsqlFragmentOrderBy"
        ],
        "properties": {
            "gctsp": {
                "python": false,
                "psql": true
            }
        }
    }
}