{
    "ORDER BY": {
        "inline": "psql_order_by({i0}, {i1})",
        "description": "PSQL ORDER BY clause with column {t0} and direction {t1}",
        "inputs": [
            "PsqlType",
            "bool"
        ],
        "outputs": [
            "PsqlFragmentOrderBy"
        ],
        "properties": {
            "gctsp": {
                "python": false,
                "psql": true
            }
        }
    },
    "ORDER BY RANDOM": {
        "inline": "PsqlFragmentOrderBy()",
        "description": "PSQL ORDER BY RANDOM() clause",
        "inputs": [],
        "outputs": [
            "PsqlFragmentOrderBy"
        ],
        "properties": {
            "gctsp": {
                "python": false,
                "psql": true
            }
        }
    }
}